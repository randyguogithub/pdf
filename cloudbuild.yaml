steps:
  # Step 1: SSH into the VM and change the working directory
  - name: gcr.io/cloud-builders/gcloud
    entrypoint: bash
    args:
      - "-c"
      - |
        gcloud compute ssh energy-audit-system --internal-ip --zone=asia-east2-c --project=randyguo-test-43165062 --command "cd /home/randyzwguo/pdf && git pull && systemctl restart energy'"
# --zone=asia-east2-b --project=randyguo-test-43165062
# Optional: Add additional steps here if needed, such as deploying the application or running commands in the VM.

timeout: "1200s"  # Set a timeout for the build process (20 minutes)